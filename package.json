{
  "name": "2026-pedal-to-the-polls",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.14"
  },
  "scripts": {
    "data:sync": "node scripts/sync-data.mjs",
    "data:check": "node --test tests/*.test.mjs",
    "dev": "npm run data:sync && vite",
    "build": "npm run data:sync && tsc -b && vite build",
    "check": "npm run data:sync && npm run data:check && tsc -b",
    "preview": "vite preview"
  },
  "dependencies": {
    "maplibre-gl": "^6.0.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
